<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CAPACITOR_DEBUG</key>
	<string>$(CAPACITOR_DEBUG)</string>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
        <string>Vero</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<!-- Stripe Terminal / Tap to Pay on iPhone requires location access; iOS hard-crashes
	     any app that touches location without this usage-description string. -->
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Vero uses your location to enable secure in-person (Tap to Pay) card payments, as required by our payment processor.</string>
	<!-- Stripe Terminal SDK requires a Bluetooth usage description; without it the SDK
	     raises an integration error and aborts at discoverReaders (SIGABRT). Required
	     even for Tap to Pay on iPhone since it shares the Terminal SDK. -->
	<key>NSBluetoothAlwaysUsageDescription</key>
	<string>Vero uses Bluetooth to connect to card readers for in-person payments.</string>
	<key>NSBluetoothPeripheralUsageDescription</key>
	<string>Vero uses Bluetooth to connect to card readers for in-person payments.</string>
	<!-- The client card's "Take photo" opens the camera via <input capture>. iOS HARD-CRASHES
	     (app closes instantly) any app that touches the camera without this usage-description
	     string — this was the "the second I click take photo the app crashes" bug. NOTE: this
	     lives in the NATIVE binary, so it only takes effect after a native rebuild
	     (npx cap sync + Xcode/TestFlight); a gotvero.com web deploy CANNOT grant it. -->
	<key>NSCameraUsageDescription</key>
	<string>Vero uses the camera to take client and gallery photos.</string>
	<!-- Some photo pickers can route through the photo library; include its usage string so
	     library access never crashes either. -->
	<key>NSPhotoLibraryUsageDescription</key>
	<string>Vero uses your photo library to attach client and gallery photos.</string>
	<!-- Stripe Terminal REQUIRES bluetooth-central in UIBackgroundModes to discover/stay
	     connected to a Bluetooth reader (Stripe Reader M2 / WisePad 3). Without it the SDK
	     raises an integration error and aborts at discoverReaders (SIGABRT) — the crash seen
	     2026-07-16 scanning for the M2. Must be an ARRAY, not a string. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>bluetooth-central</string>
	</array>
	<key>UILaunchStoryboardName</key>
	<string>LaunchScreen</string>
	<key>UIMainStoryboardFile</key>
	<string>Main</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>armv7</string>
	</array>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UISupportedInterfaceOrientations~ipad</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
		<string>UIInterfaceOrientationPortraitUpsideDown</string>
		<string>UIInterfaceOrientationLandscapeLeft</string>
		<string>UIInterfaceOrientationLandscapeRight</string>
	</array>
	<key>UIViewControllerBasedStatusBarAppearance</key>
	<true/>
</dict>
</plist>
